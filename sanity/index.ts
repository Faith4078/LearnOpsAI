/**
 * Sanity schema entry point. A Sanity Studio config can consume this as
 * `schema: { types: schemaTypes }`.
 */
export { helpArticle } from "./schemas/help-article";

import { helpArticle } from "./schemas/help-article";

export const schemaTypes = [helpArticle];
